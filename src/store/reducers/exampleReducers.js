export const example1 = (state = 'Hello', action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export const example2 = (state = 'World!', action) => {
  switch (action.type) {
    default:
      return state;
  }
};
